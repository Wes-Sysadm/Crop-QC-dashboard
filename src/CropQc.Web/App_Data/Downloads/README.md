# Admin Downloads

Place internal installer files for Admin-only downloads here during deployment.

Expected file for the FTA proof-of-concept:

- `FTADLL.exe`

Do not commit installer binaries unless the team explicitly approves storing them in the repository.
